version: '3.8'

services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile.simple
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=temp-secret-key-for-testing-only-change-in-production
      - SERVER_PORT=8080
      - COOKIE_SECURE=false