version: '3.8'

services:
  salaf-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - DB_URL=jdbc:sqlite:/app/data/salaf.db
      - JWT_SECRET=${JWT_SECRET:-your-secure-256-bit-secret-key-change-in-production}
      - JWT_EXPIRATION_MS=${JWT_EXPIRATION_MS:-86400000}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - LOG_LEVEL_ROOT=${LOG_LEVEL_ROOT:-INFO}
      - LOG_LEVEL_SECURITY=${LOG_LEVEL_SECURITY:-WARN}
    volumes:
      - salaf_data:/app/data
      - salaf_logs:/app/logs
    restart: unless-stopped

volumes:
  salaf_data:
    driver: local
  salaf_logs:
    driver: local