version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=your-secure-256-bit-secret-key-change-in-production
      - SERVER_PORT=8080
      - COOKIE_SECURE=false